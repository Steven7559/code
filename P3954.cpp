#include <bits/stdc++.h>
using namespace std;
double a, b, c;
int main() {
    // freopen("filename.in","r",stdin);
    // freopen("filename.out","w",stdout);
    cin >> a >> b >> c;
    cout << 0.2 * a + 0.3 * b + 0.5 * c;
    return 0;
}