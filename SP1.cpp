#include <bits/stdc++.h>
#define int long long
using namespace std;
signed main() {
    while (1) {
        int x;
        cin >> x;
        if (x == 42)
            return 0;
        cout << x << endl;
    }
    return 0;
}